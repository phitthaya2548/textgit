import 'package:flutter/material.dart';

class ManageUsersView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('จัดการผู้ใช้งาน')),
      body: Center(child: Text('แสดงรายการผู้ใช้งาน')),
    );
  }
}
